    spring.application.name=careconnect-backend

    # Database Configuration
    spring.datasource.url=jdbc:postgresql://careconnect-db.cbw6u68qs0cv.us-east-2.rds.amazonaws.com:5432/careconnect
    spring.datasource.username=postgres
    spring.datasource.password=828798boM
    spring.datasource.driver-class-name=org.postgresql.Driver

    # JPA / Hibernate Configuration
    spring.jpa.hibernate.ddl-auto=update
    spring.jpa.show-sql=true
    spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

    # Optional: Server port (defaults to 8080)
    # server.port=8080

    spring.mail.host=smtp.gmail.com
    spring.mail.port=587
    spring.mail.username=bomplar@gmail.com
    spring.mail.password=qvqfivjiostlhmnp
    spring.mail.properties.mail.smtp.auth=true
    spring.mail.properties.mail.smtp.starttls.enable=true
    logging.level.org.springframework.web=DEBUG

    server.servlet.session.cookie.http-only=true
    server.servlet.session.cookie.secure=false

# Enables cross-origin session cookie sharing
    server.servlet.session.cookie.same-site=none

    careconnect.upload.dir=C:/Users/bompl/Documents/uploads