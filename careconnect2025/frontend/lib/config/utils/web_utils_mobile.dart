/// Mobile stub for web utilities
/// These functions do nothing on mobile platforms
class WebUtils {
  /// Enable viewport meta for better responsive behavior on web (mobile stub)
  static void configureWebViewport() {
    // No-op on mobile
  }

  /// Disable text selection on web (mobile stub)
  static void disableTextSelection() {
    // No-op on mobile
  }

  /// Enable scrollbar customization on web (mobile stub)
  static void customizeScrollbars() {
    // No-op on mobile
  }

  /// Set theme color for browser UI (mobile stub)
  static void setThemeColor(String color) {
    // No-op on mobile
  }

  /// Add web-specific CSS styles (mobile stub)
  static void addWebStyles() {
    // No-op on mobile
  }

  /// Configure web app manifest properties (mobile stub)
  static void configureWebApp() {
    // No-op on mobile
  }

  /// Initialize all web optimizations (mobile stub)
  static void initializeWebOptimizations() {
    // No-op on mobile
  }
}
