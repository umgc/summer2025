# Environment Configuration Template
# Copy this file to .env.local and fill in your actual values
# NEVER commit files with real secrets to version control!

# =============================================================================
# 🔐 SECURITY WARNING
# =============================================================================
# This is a template file. Replace ALL placeholder values with real ones.
# Copy to .env.local (which is gitignored) for local development.

# =============================================================================
# FRONTEND CONFIGURATION (Flutter App)
# =============================================================================

# API Configuration
CC_BASE_URL_WEB=http://localhost:8080
CC_BASE_URL_ANDROID=http://10.0.2.2:8080
CC_BASE_URL_OTHER=http://localhost:8080

# AI Service Configuration (SECURE THESE!)
OPENAI_API_KEY=your_openai_api_key_here
DEEPSEEK_API_KEY=your_deepseek_api_key_here
deepSeek_uri=https://api.deepseek.com/v1/chat/completions

# Authentication (SECURE THESE!)
JWT_SECRET=your_secure_jwt_secret_32_chars_minimum
CC_BACKEND_TOKEN=your_backend_token_here

# =============================================================================
# PRODUCTION ENVIRONMENT VARIABLES
# =============================================================================
# For production, set these in your hosting platform's environment settings:
# - Vercel: Project Settings > Environment Variables
# - Heroku: heroku config:set VAR_NAME=value
# - AWS: Parameter Store or Secrets Manager
# - GitHub Actions: Repository Settings > Secrets

# =============================================================================
# SECURITY REQUIREMENTS
# =============================================================================

# 1. Generate secure JWT secret:
#    openssl rand -base64 64

# 2. Google OAuth2 Setup:
#    - Go to Google Cloud Console
#    - Create OAuth2 credentials
#    - Set authorized redirect URIs:
#      * http://localhost:8080/oauth2/callback/google (development)
#      * https://yourdomain.com/oauth2/callback/google (production)

# 3. Environment-specific files:
#    - .env.development
#    - .env.production
#    - .env.staging

# 4. Never commit these files:
#    Add to .gitignore:
#    .env*
#    *.secret
#    credentials.json
